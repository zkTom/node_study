# 安装 typescript

```
npm install -g typescript
// 查看版本指令
tsc -v
// 安装库的d.ts文件,如安装node.d.ts文件
npm install --save -g @types/node
// 初始化package.json
npm init -y
// 初始化tsconfig.json
tsc --init
```
